package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dark palette ──────────────────────────────────────────────────────────────
val DarkBg           = Color(0xFF0C0C14)   // deep space background
val DarkSurface      = Color(0xFF13131F)   // elevated surface / cards
val DarkSurfaceCard  = Color(0xFF1C1C2E)   // deeper card / input bg

val VioletPrimary    = Color(0xFF7C6FFF)   // vivid violet – main accent
val MintSecondary    = Color(0xFF00C9A7)   // mint teal – secondary accent
val CoralTertiary    = Color(0xFFFF6B6B)   // coral – recording / warning

val DarkBorder       = Color(0xFF2A2A3E)   // subtle border
val DarkError        = Color(0xFFFF5370)   // error red
val IceText          = Color(0xFFEEEEF2)   // primary text
val FogText          = Color(0xFF8B8B9E)   // muted / subtitle text
val GoldAccent       = Color(0xFFFFD166)   // amber highlight

// ── Light palette ─────────────────────────────────────────────────────────────
val LightBg          = Color(0xFFF6F6FB)
val LightSurface     = Color(0xFFFFFFFF)
val LightSurfaceCard = Color(0xFFF0F0F8)
val VioletLight      = Color(0xFF5A4FD3)
val MintLight        = Color(0xFF007A6A)
val CoralLight       = Color(0xFFB5003C)
val LightBorder      = Color(0xFFE0E0EE)
val LightError       = Color(0xFFB5003C)
val DarkText         = Color(0xFF14141E)
val MutedText        = Color(0xFF55556A)
